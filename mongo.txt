docker exec -it lingo-db mongosh
docker exec -it lingo-db mongosh -u admin -p password --authenticationDatabase admin

// 1. Show all databases
show dbs

// 2. Switch to your app's database
use LingoApp

// 3. Show all tables (collections) inside LingoApp
show collections

// 4. Show the first 20 users (assuming you have a 'users' collection)
db.users.find().pretty()

// 5. Count how many users exist
db.users.countDocuments()

// 6. Show the first 20 items in another collection (replace 'exercises' with actual name)
db.exercises.find().pretty()
